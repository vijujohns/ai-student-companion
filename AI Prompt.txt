You are an expert AI solution architect and Python full-stack developer. 

I want you to **build a production-ready, fully executable AI Student Tutor system** that can be run locally on Windows 11 (i5 CPU, 16GB RAM). The system must be fully modular, CPU-optimized, offline-capable, and production-quality.

**Requirements:**
- Multi-class, multi-subject, chapter-level content
- PDF ingestion (text + images)
- Text + image embeddings with FAISS vector store
- RAG orchestration for queries
- Step-by-step solution module for Math/Physics/Chemistry
- Multilingual support (Hindi, Sanskrit, English) with translations
- Flashcards, quizzes, summaries generation
- Short video/image content generation (placeholder)
- Progress tracking, save/retrieve results
- Model manager: dynamic onboarding, student-selectable model
- Frontend: PyQt GUI with:
  - Class → Subject → Chapter selection
  - Model selection dropdown
  - PDF viewer
  - Question input
  - Answer display (text + image + step-by-step)
  - Flashcards, summaries, quizzes
  - Progress dashboard
  - Content generation panel
  - Save/retrieve results

**Deliverables:**
1. Complete project folder structure with all necessary Python files.
2. Backend and frontend fully coded, ready to execute.
3. Sample PDFs and placeholder images in `knowledge_base/`.
4. `requirements.txt` for dependencies.
5. `README.md` with full instructions for local setup and execution.
6. Metadata handled correctly for class, subject, chapter, language, type, step-by-step, and model used.
7. Provide **all files as a ZIP** structure, ready to download and run locally.
8. Ensure code is **production-quality**, not sample snippets, and fully runnable.

**Output:**
- Provide the **entire folder/file structure** with code contents.
- Include all Python modules, GUI code, ingestion scripts, embeddings, FAISS setup, RAG orchestration, step-by-step solver, multilingual translation, flashcards/quiz generator, content generator, progress tracker, and model manager.
- Include comments, instructions, and placeholders where needed (images/videos).
- Ensure system can be launched with a single command, e.g., `python main.py`.

**Build the full solution now and provide it as a ZIP-ready folder structure with all files and code.**